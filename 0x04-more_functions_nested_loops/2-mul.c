#include "main.h"
/**
 * mul - multiplies two integers
 * @i:  the 1st number to be multiplied
 * @j: the 2nd number to be multiplied
 * Return:  the  product of the two numbers
 */

int mul(int i, int j)
{
	return (i * j);
}
